# Review history — wired-now-display-windows

Append-only. `sites.json` holds the current state; this file holds how it got
there. Alex's wording is never edited, and agent observations are never merged
into his judgement.

---

## 2026-08-31 · ADDED

- **action:** entered as `kind: print-composition` — a print spread kept for its
  compositional and typographic thinking, not as a site
- **source:** magazine feature spread
- **rating / dialectStatus:** **null / unreviewed.** Both are Alex's and neither is
  set here.

### Agent observation — not Alex's judgement

| Layer | Observation | Source | Verdict |
|---|---|---|---|
| typography · composition | Three didone letters are cut through to the photograph beneath, so the image reads inside the letterforms and the ground reads between them. The message itself is set at micro scale on the centre line — the smallest type on the spread carries the sentence and the largest carries the word. | agent | unreviewed |

### Tags

Typography tags were applied as free additions, which the schema permits. **None
were promoted into `vocab.json`** — promotion is deliberate and Alex's. Seven of
them are new terms naming what type *does* rather than what it looks like:
`type-as-container`, `type-as-window`, `type-as-mass`, `type-interrupted`,
`type-crossing-subject`, `type-as-frame`, `numeral-as-anchor`; four more name
paragraph-level work: `run-in-opener`, `dual-measure`, `tracking-as-texture`,
`concept-in-the-body`.

**No composition tags were applied.** The schema is explicit that they mean a
quality is present *and a human has confirmed it*, and they are never inferred.

### No evidence attached

`shots` is `null` with a recorded reason. The spread arrived as an image in
conversation and was never written to disk, so this entry carries reasoning and no
evidence. It cannot be cited in a Design Read until a scan sits in
`vault/shots/wired-now-display-windows/`.

### Still owed

`rating`, `dialectStatus` and `note` — all three are Alex's, and nothing here fills
them in.

### Alex's words on the set — verbatim, never edited

Said on 2026-08-31 while sending this group. They are recorded here because they
are his and they are judgement; they are **not** converted into a rating. The
oilstainlab record already carries a rating inferred from an enthusiastic sentence
with a note saying to revert it if that read it too strongly — the same mistake is
not repeated here.

> вот что мне нравится, но это magazine верстака. хотя она сейчас модная. я разные
> сделал газету и журналы....

> игра с текстом, крутые приёмы, мелкая детализация в украшение страницы, мелкая
> детализация, возможно больше даже декоративная но все с умом, сбалансировано.
> совмещение не совмещаемого иногда.

> печать как композиция для web я бы так назвал и то не всегда применяю

> тут больше как работают с текстом а не про переносы контента. работа с текстом и
> композиция

The third line is why `kind` is `print-composition` rather than `print`: what is
being collected is the compositional and typographic thinking, and his own
qualification — that it does not always apply — travels with it.
