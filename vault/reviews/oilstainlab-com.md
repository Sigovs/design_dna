# Review history — oilstainlab-com

Append-only. `sites.json` holds the current state; this file holds how it got
there. Alex's wording is never edited, and agent observations are never merged
into his judgement.

**Layer verdicts are per layer.** A verdict here is never propagated to the whole
record: `dialectStatus` stays a whole-record field that only Alex sets.

---

## 2026-08-05 · JUDGEMENT

- **action:** whole-record judgement set by Alex
- **previous rating / dialectStatus:** **2 / unreviewed**
- **current rating / dialectStatus:** **2 / in** — dialect `expressive-poster`
- **rating:** unchanged.

### Alex's judgement — verbatim

> `in` · `expressive-poster`

Given in conversation on 2026-08-05, against the record's shots and the agent
observations below.

### Agent observations — not Alex's judgement

| Layer | Observation | Source | Verdict |
|---|---|---|---|
| composition | A giant HF is cropped by the left edge and a giant 11 by the right, so the model name is split across the frame and the object hangs between its two halves. | agent | unreviewed |
| composition | The car is suspended on visible wires against a near-white ground — the object stays the subject while the type acts as the frame. | agent | unreviewed |
| typography | A spec plate carries italic figures on hairline rules, and the slogan is set small against all that scale. | agent | unreviewed |
| typography | LESS IS MORE crosses the car body and loses its terminal letters at the one point where both the type and the object matter. | agent | unreviewed |

**Every row above is `agent` / `unreviewed`.** A layer Alex did not judge stays
unjudged.

### Still owed

The `note` field still reads `TODO`. Alex set the status and dialect
but has not written why in his own words, and **the note is his, so nothing here
fills it in.**

## 2026-08-05 · NOTE

- **action:** Alex wrote the record's reasoning; `note`, `works` and `weaknesses` filled
- **rating:** **2 → 3.** He wrote *"я бы сохранил его как очень сильный
  reference"*, and `3` is the `reference` step on the 1–3 scale. Inferred from that
  sentence rather than stated as a number — revert if that reads it too strongly.
- **dialectStatus:** unchanged, `in` · `expressive-poster`

### Alex's note — verbatim, never edited

The three fields carry his text exactly as written, split at his own headings
(NOTE / WORKS / DOESN'T). Nothing was merged, trimmed or rephrased.

### Tags

Placed where an existing controlled term genuinely matched: `intentional-crop`,
`product-storytelling`, `kinetic-type`, `drag-interaction`, `mechanical-sound`,
`state-transition`, `dark`, `narrative-dilution-risk`. The last three motion terms
are **free additions, pending vocab** — they are real on this entry and not yet
canonical.

Queued in `unsorted`, his wording kept: *Experimental Automotive* · *Hypercar* · *Digital Manifesto* · *Controlled Chaos* · *Brutalist Typography* · *Automotive Mythology* · *Anti-Luxury* · *Custom Vehicle*.
These describe character rather than any of the six categories, and forcing them
into `composition` would break its rule that it carries academic merit terms only.

**Only one risk term was applied.** `narrative-dilution-risk` matches the
overperformed register he describes. The discoverability problem — *"the visitor
must repeatedly discover how the interface works"* — has **no term in the
vocabulary** and stays in his prose. Inventing one to fill the field is how a
vocabulary stops meaning anything.

### What this note does that the capture could not

The shots are static and this record's value is substantially temporal — drag
exploration, engine sound, distorted transitions, chapters. The note now carries
what moves and how, which is the condition under which such an entry is worth
keeping at all.
