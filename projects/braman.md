---
project:      braman
client:       Braman
date:         not recorded
mandate:      not recorded
carried:      not recorded
delivery:     not recorded
style_mode:   not recorded
dialect:      not recorded
anchor:       not recorded
contrast:     not recorded
signature:    not recorded
dialect_fit:  not recorded
suspended:    not recorded
role:         not recorded
register:
  ground:     white
  display:    Bentley (client typeface)
  accent:     not recorded
  image:      mixed
outcome:      not recorded
---

> **Reconstructed 2026-08-04, from the build only.** Every field above was read out
> of the code; everything unreadable is `not recorded` rather than guessed.

## 1. The brief in my own words

not recorded.

## 2. Task and means

- **Task:** not recorded. Three HTML pages — `index`, `index2`, `index3` — with no
  named routes, so the page set says nothing about what the visitor came to do.
- **Means:** not recorded.

## 3. Decisions

not recorded.

## 3b. Exploration, if one ran

not recorded. Three unnamed index variants are consistent with directions built
side by side, but nothing distinguishes a variant from a superseded draft here.

## 4. Environment knowledge

- **Build-free**, no `package.json`. Three HTML pages, three JS files, five CSS
  files, `system.css` carrying 80 custom properties.
- **This is the one build in the corpus working inside somebody else's brand
  system.** The type token is `--font: "Bentley"` — the manufacturer's own
  licensed face — and the ground tokens are named for it: `--ground-deep` resolves
  to `--bentley-green`, `--ground-quiet` to `--satin-linen-40`. The register here
  is not a choice this studio made, and it must not be read as taste evidence or
  counted as drift against the others.
- No external library is loaded by script tag.

## 5. What turned out wrong

not recorded.
